import React from 'react'
import './CommonSection.css'

const CommonSection = ({title}) => {
  return (
    <section className='full common-section'>
        <h1>{title}</h1>
    </section>
  )
}

export default CommonSection
